<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Cart - Sahana Restaurant</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
  <!-- Animate.css -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
  
  <style>
    :root {
      --primary: #7D4C2A;
      --secondary: #D4AF37;
      --success: #50A162;
      --danger: #C75D4C;
      --light: #FAF3E0;
      --dark: #2C1E10;
      --white: #FFFFFF;
      --veg: #50A162;
      --nonveg: #C75D4C;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #FAF3E0 0%, #F5EAD8 100%);
      background-attachment: fixed;
      color: var(--dark);
      min-height: 100vh;
      position: relative;
      padding-bottom: 60px;
    }
    
    /* Header styling with restaurant hero image */
    .restaurant-header {
      background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.7)), 
                  url('https://images.unsplash.com/photo-1514933651103-005eec06c04b?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
      background-size: cover;
      background-position: center;
      color: var(--white);
      padding: 4rem 0;
      margin-bottom: 2.5rem;
      position: relative;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      border-bottom-left-radius: 50% 5%;
      border-bottom-right-radius: 50% 5%;
    }
    
    .header-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, rgba(125, 76, 42, 0.7), rgba(44, 30, 16, 0.8));
      z-index: 1;
    }
    
    .restaurant-header .container {
      position: relative;
      z-index: 2;
    }
    
    .logo-text {
      font-family: 'Playfair Display', serif;
      font-size: 3.2rem;
      font-weight: 700;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
      margin-bottom: 5px;
    }
    
    .tagline {
      font-size: 1.1rem;
      font-weight: 300;
      letter-spacing: 2px;
      text-transform: uppercase;
      opacity: 0.9;
    }
    
    .page-title {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      font-weight: 600;
      margin-bottom: 2rem;
      color: var(--primary);
      text-align: center;
      position: relative;
      padding-bottom: 15px;
    }
    
    .page-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 3px;
      background: var(--secondary);
      border-radius: 2px;
    }
    
    /* Main content container */
    .main-container {
      max-width: 900px;
      margin: 0 auto;
      padding: 0 20px;
    }
    
    /* Empty cart styling */
    .empty-cart {
      text-align: center;
      padding: 3rem 2rem;
      border-radius: 15px;
      background-color: var(--white);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      margin-bottom: 2rem;
    }
    
    .empty-cart i {
      font-size: 5rem;
      color: var(--secondary);
      opacity: 0.6;
      margin-bottom: 1.5rem;
    }
    
    .empty-cart p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      color: #777;
    }
    
    /* Cart items styling */
    .cart-container {
      border-radius: 15px;
      background-color: var(--white);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      margin-bottom: 2rem;
    }
    
    .cart-items-header {
      background-color: var(--primary);
      color: var(--white);
      padding: 1.2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .cart-items-title {
      font-family: 'Playfair Display', serif;
      font-size: 1.4rem;
      margin: 0;
      font-weight: 600;
    }
    
    .cart-count {
      background-color: var(--secondary);
      color: var(--dark);
      padding: 0.2rem 0.8rem;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 600;
    }
    
    .cart-item {
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
      padding: 1.5rem;
      transition: all 0.3s ease;
      animation: fadeIn 0.6s;
    }
    
    .cart-item:last-child {
      border-bottom: none;
    }
    
    .cart-item:hover {
      background-color: rgba(250, 243, 224, 0.3);
    }
    
    .item-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 0.8rem;
    }
    
    .item-name {
      font-size: 1.3rem;
      font-weight: 600;
      color: var(--dark);
      margin: 0;
    }
    
    .item-price {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--primary);
      font-family: 'Playfair Display', serif;
    }
    
    .item-details {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.5rem;
      color: #777;
    }
    
    .item-type {
      display: flex;
      align-items: center;
      font-size: 0.95rem;
    }
    
    .item-type i {
      margin-right: 5px;
    }
    
    .veg {
      color: var(--veg);
      font-weight: 600;
    }
    
    .nonveg {
      color: var(--nonveg);
      font-weight: 600;
    }
    
    .item-prep-time {
      display: flex;
      align-items: center;
      font-size: 0.95rem;
    }
    
    .item-prep-time i {
      margin-right: 5px;
    }
    
    .remove-btn {
      background-color: var(--white);
      color: var(--danger);
      border: 1px solid var(--danger);
      border-radius: 8px;
      padding: 0.5rem 1rem;
      font-size: 0.9rem;
      cursor: pointer;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      margin-top: 0.5rem;
    }
    
    .remove-btn:hover {
      background-color: var(--danger);
      color: var(--white);
      transform: translateY(-2px);
    }
    
    /* Total section styling */
    .total-section {
      background-color: var(--light);
      padding: 1.5rem;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .total-text {
      font-size: 1.3rem;
      font-weight: 600;
      color: var(--dark);
    }
    
    .total-amount {
      font-family: 'Playfair Display', serif;
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary);
    }
    
    /* Order form styling */
    .order-form {
      background-color: var(--white);
      padding: 2rem;
      border-radius: 15px;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.08);
      margin-bottom: 2rem;
    }
    
    .form-section-title {
      font-family: 'Playfair Display', serif;
      font-size: 1.5rem;
      color: var(--primary);
      margin-bottom: 1.5rem;
      font-weight: 600;
    }
    
    .form-group {
      margin-bottom: 1.5rem;
    }
    
    .form-label {
      font-weight: 500;
      margin-bottom: 0.5rem;
      display: block;
      color: var(--dark);
    }
    
    .form-control {
      border: 2px solid #e2e2e2;
      border-radius: 10px;
      padding: 0.8rem 1rem;
      transition: all 0.3s ease;
    }
    
    .form-control:focus {
      border-color: var(--secondary);
      box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.2);
    }
    
    .place-order-btn, .continue-shopping {
      border: none;
      border-radius: 10px;
      padding: 0.9rem 1.5rem;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
    
    .place-order-btn {
      background: linear-gradient(to right, var(--success), #6ABE76);
      color: var(--white);
      box-shadow: 0 8px 15px rgba(80, 161, 98, 0.2);
    }
    
    .place-order-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(80, 161, 98, 0.3);
    }
    
    .continue-shopping {
      background: linear-gradient(to right, var(--primary), #8D5C3A);
      color: var(--white);
      box-shadow: 0 8px 15px rgba(125, 76, 42, 0.2);
    }
    
    .continue-shopping:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(125, 76, 42, 0.3);
      color: var(--white);
    }
    
    /* Footer styling */
    .footer {
      background-color: var(--dark);
      color: var(--light);
      padding: 1rem 0;
      text-align: center;
      position: absolute;
      bottom: 0;
      width: 100%;
      font-size: 0.9rem;
    }
    
    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .animate-fade-in {
      animation: fadeIn 1s;
    }
    
    /* Responsive styles */
    @media (max-width: 768px) {
      .logo-text {
        font-size: 2.5rem;
      }
      
      .tagline {
        font-size: 0.95rem;
      }
      
      .page-title {
        font-size: 2rem;
      }
      
      .item-header {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .item-price {
        margin-top: 0.5rem;
      }
      
      .item-details {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .item-prep-time {
        margin-top: 0.5rem;
      }
      
      .total-section {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .total-amount {
        margin-top: 0.5rem;
      }
      
      .btn-group {
        flex-direction: column;
        width: 100%;
      }
      
      .place-order-btn, .continue-shopping {
        width: 100%;
        margin-bottom: 1rem;
      }
    }
  </style>
</head>
<body>
  <!-- Restaurant Header -->
  <header class="restaurant-header animate__animated animate__fadeIn">
    <div class="header-overlay"></div>
    <div class="container text-center">
      <h1 class="logo-text">Sahana Restaurant</h1>
      <p class="tagline">Exquisite Culinary Experience</p>
    </div>
  </header>

  <div class="main-container">
    <h2 class="page-title animate__animated animate__fadeInUp">Your Cart</h2>
    
    <% if (!cart || cart.length === 0) { %>
      <!-- Empty Cart Display -->
      <div class="empty-cart animate__animated animate__fadeIn">
        <i class="fas fa-shopping-cart"></i>
        <h3>Your cart is empty</h3>
        <p>Add some delicious items from our menu and come back!</p>
        <a href="/" class="continue-shopping">
          <i class="fas fa-utensils"></i> Browse Menu
        </a>
      </div>
    <% } else { %>
      <!-- Cart Items Display -->
      <div class="cart-container animate__animated animate__fadeIn">
        <div class="cart-items-header">
          <h3 class="cart-items-title">Cart Items</h3>
          <span class="cart-count"><%= cart.length %> items</span>
        </div>
        
        <% cart.forEach((item, index) => { %>
          <div class="cart-item">
            <div class="item-header">
              <h4 class="item-name"><%= item.name %></h4>
              <div class="item-price">₹<%= parseFloat(item.price).toFixed(2) %></div>
            </div>
            
            <div class="item-details">
              <div class="item-type">
                <% if(item.type === 'veg') { %>
                  <i class="fas fa-leaf veg"></i>
                  <span class="veg">Vegetarian</span>
                <% } else { %>
                  <i class="fas fa-drumstick-bite nonveg"></i>
                  <span class="nonveg">Non-Vegetarian</span>
                <% } %>
              </div>
              
              <div class="item-prep-time">
                <i class="far fa-clock"></i>
                <span>Preparation Time: <%= item.prepTime %> mins</span>
              </div>
            </div>
            
            <form action="/remove-from-cart" method="POST">
              <input type="hidden" name="index" value="<%= index %>">
              <button type="submit" class="remove-btn">
                <i class="fas fa-trash-alt"></i> Remove
              </button>
            </form>
          </div>
        <% }) %>
      </div>
      
      <!-- Total Section -->
      <div class="total-section animate__animated animate__fadeInUp" data-wow-delay="0.3s">
        <div class="total-text">Total Amount</div>
        <div class="total-amount">₹<%= cart.reduce((sum, item) => sum + parseFloat(item.price), 0).toFixed(2) %></div>
      </div>
      
      <!-- Order Form -->
      <div class="order-form animate__animated animate__fadeInUp" data-wow-delay="0.5s">
        <h3 class="form-section-title">Complete Your Order</h3>
        
        <form action="/place-order" method="POST">
          <div class="row">
            <div class="col-md-6">
              <div class="form-group">
                <label for="tableNumber" class="form-label">Table Number</label>
                <input type="number" id="tableNumber" name="tableNumber" class="form-control" required>
              </div>
            </div>
            
            <div class="col-md-6">
              <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" id="email" name="email" class="form-control" required placeholder="For order confirmation">
              </div>
            </div>
          </div>
          
          <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
            <a href="/" class="continue-shopping me-md-2">
              <i class="fas fa-arrow-left"></i> Continue Shopping
            </a>
            <button type="submit" class="place-order-btn">
              <i class="fas fa-check-circle"></i> Place Order
            </button>
          </div>
        </form>
      </div>
    <% } %>
  </div>
  
  <!-- Footer -->
  <footer class="footer">
    <p>© <%= new Date().getFullYear() %> Sahana Restaurant. All rights reserved.</p>
  </footer>

  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  
  <!-- Custom JavaScript for animations -->
  <script>
    // Add smooth scrolling
    document.querySelectorAll('a[href^="/#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        document.querySelector(this.getAttribute('href')).scrollIntoView({
          behavior: 'smooth'
        });
      });
    });
    
    // Add animation delay for items
    document.addEventListener('DOMContentLoaded', function() {
      // Add staggered animation to cart items
      const cartItems = document.querySelectorAll('.cart-item');
      cartItems.forEach((item, index) => {
        item.style.animationDelay = `${0.1 * (index + 1)}s`;
      });
    });
  </script>
</body>
</html>